# Ensure package-lock.json is generated with exact versions
package-lock=true

# Use package-lock.json for deterministic installs
package-lock-only=false

# Ensure package-lock.json is generated with the same version of npm
save-exact=true

# Enable strict SSL
strict-ssl=true

# Set the engine-strict flag
engine-strict=true
